# 終了時待ち台数ペナルティ 実装報告書

## 概要

シミュレーション終了時点の待ち台数を時間換算してペナルティとして目的関数に追加する機能を実装しました。

## 実装内容

### 変更ファイル

#### [cost_calculator.py](file:///home/oums/Desktop/emates/02_src/util/cost_calculator.py)

**1. 定数追加**
```python
AVG_CHARGING_TIME = 0.5  # 平均充電時間（時間）- 終了時待ち台数ペナルティ用
```

**2. 新規関数追加**
```python
def calc_ending_waiting_penalty(waiting_line: pd.DataFrame) -> float:
    """終了時の待ち台数を時間換算してペナルティ計算"""
```

**3. evaluation_total_costs() 修正**
- `waiting_line` データの取得を追加
- ペナルティ計算・割引計算を総コストに統合

---

## ペナルティ計算式

```
ペナルティ = 終了時待ち台数 × 平均充電時間(0.5h) × 2(待ち重み) × 時間価値 × 365日
```

---

## 動作確認結果

```
終了時待ち台数: 10 台
推定待ち時間: 5.0 時間
待ち台数ペナルティ: 408.1 万円/年
```

✅ 期待値（10 × 0.5 × 2 × 0.1118 × 365 ≈ 407万円）と一致

---

## 影響範囲

- **最適化実行**: 容量不足のCS配置は自動的にペナルティを受け、排除される
- **既存機能**: 変更なし（追加機能のみ）
- **シミュレータ**: 変更不要（既存の `charging_info.bin` を使用）

---

## 次のステップ

1. [ ] 複数CS配置パターンでの動作確認
2. [ ] 最適化実行時のペナルティ効果検証
3. [ ] 必要に応じて `AVG_CHARGING_TIME` の調整
